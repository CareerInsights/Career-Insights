import type { Config } from 'tailwindcss'

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        chloe: ['Chloe', 'sans-serif'],
      },
    },
  },
  plugins: [],
}

export default config
